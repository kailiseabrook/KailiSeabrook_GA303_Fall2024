using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script1 : MonoBehaviour
{
    public char a = 'a' ;
    public char w = 'w' ;
    public char e = 'e' ;
    public char s = 's' ;
    public char o = 'o' ;
    public char m = 'm' ;

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(a.ToString() + w.ToString() + e.ToString() + s.ToString()+ o.ToString() + m.ToString() + e.ToString());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
